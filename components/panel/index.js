import styled from 'styled-components';
import styles from './index.css';

const PanelWrapper = styled.div`${styles}`;

export default () => (
    <PanelWrapper>
        <p>Panel will come here with content</p>
    </PanelWrapper>
)